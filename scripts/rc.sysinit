#!/bin/bash
#
# Common system initialisation script
#

### Load locale
if [ -z "${LANG}" ]; then
    source "£{ETC}/profile.d/read_locale.sh"
fi

### Override process with rc.sysinit.py
exec "£{PREFIX}${LIBEXEC}/rc.sysinit.py"

